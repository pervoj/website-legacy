backend:
  name: git-gateway
  branch: main

media_folder: "static/images/posts"
public_folder: "/images/posts"

collections:

  - name: "blog-en"
    label: "Blog (en)"
    folder: "content/blog"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{fields.slugurl}}"
    filter:
      field: lang
      value: en
    fields:
      - { label: "Language", name: "lang", widget: "hidden", default: "en" }
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Slug", name: "slugurl", widget: "string", pattern: ["^[A-Za-z0-9\s]{1,}$", "Slug can't contain spaces!"] }
      - { label: "Description", name: "extra.description", widget: "string", required: false }
      - { label: "Date", name: "date", widget: "datetime", time_format: false }
      - label: "Extra"
        name: "extra"
        widget: "object"
        fields:
          - { label: "Image", name: "image", widget: "image", required: false }
      - { label: "Body", name: "body", widget: "markdown" }

  - name: "blog-cs"
    label: "Blog (cs)"
    folder: "content/blog"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{fields.slugurl}}.cs"
    filter:
      field: lang
      value: cs
    fields:
      - { label: "Language", name: "lang", widget: "hidden", default: "cs" }
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Slug", name: "slugurl", widget: "string", pattern: ["^[A-Za-z0-9\s]{1,}$", "Slug can't contain spaces!"] }
      - { label: "Description", name: "extra.description", widget: "string", required: false }
      - { label: "Date", name: "date", widget: "datetime", time_format: false }
      - label: "Extra"
        name: "extra"
        widget: "object"
        fields:
          - { label: "Image", name: "image", widget: "image", required: false }
      - { label: "Body", name: "body", widget: "markdown" }
